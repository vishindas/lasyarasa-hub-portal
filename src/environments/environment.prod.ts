export const environment = {
  production: true,
  apiUrl: 'https://app.lasyarasahub.com/api',
  // See environment.ts for the full rationale. Committed false here too --
  // enabling the entry point is a later, explicit, separately-authorized
  // change to this literal value, not a runtime toggle.
  studentLearningEntryEnabled: false
};
